export const CATEGORIES = ['Food', 'Transport', 'Housing', 'Entertainment', 'Other']

// Monthly budget per category, in dollars. Food is intentionally low so the
// over-budget indicator in the Summary view is easy to exercise.
export const BUDGETS = {
  Food: 50,
  Transport: 100,
  Housing: 1000,
  Entertainment: 80,
  Other: 200,
}
