# Task Type D grader dependencies.
# Install browsers once with: python -m playwright install chromium
click>=8.0.0
playwright>=1.40.0
# Optional: only needed for --judge-model (LLM-as-a-judge).
openai>=1.0.0
